// This was written with node 10.1.0 installed! If you don't have it, you'll need to use Babel to transpile it to regular ES5
var name = 'John Doe';

var greet = 'Hello ' + name;
var greet2 = `Hello ${name}`;

console.log(greet);
console.log(greet2);